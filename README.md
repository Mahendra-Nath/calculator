## Running the Application

Command to execute the program after the container is up and running:

```bash
docker exec -it Calculator java -jar Calculator-1.0-SNAPSHOT-jar-with-dependencies.jar
```

test 3